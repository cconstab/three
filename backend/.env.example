# Environment variables for development
NODE_ENV=development
PORT=3001

# Database configuration (use 'database' for Docker, 'localhost' for local dev)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=taskmanager
DB_USER=taskuser
DB_PASSWORD=taskpass123

# Frontend URL for CORS (adjust for your deployment environment)
FRONTEND_URL=http://localhost:3000

# Additional CORS origins (uncomment and modify as needed for Linux deployment)
# FRONTEND_URL_ALT=http://127.0.0.1:3000
# FRONTEND_URL_PUBLIC=http://your-server-ip:3000
# FRONTEND_URL_DOMAIN=http://your-domain.com:3000