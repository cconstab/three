# Example Environment Configuration
# Copy this file to .env and customize for your deployment

# Host IP/Hostname Configuration - CHANGE THIS TO YOUR SERVER'S IP OR HOSTNAME
# Examples:
# HOST_IP=192.168.1.100        # IP address
# HOST_IP=myserver.local       # Local hostname
# HOST_IP=server.example.com   # Domain name
# HOST_IP=10.0.0.50           # Internal IP
HOST_IP=cally.lan

# External Port Configuration (change if port 3000 conflicts with other services)
# EXTERNAL_PORT=3000          # Default port (can conflict with other dev servers)
# EXTERNAL_PORT=8080          # Alternative port
# EXTERNAL_PORT=5000          # Another common alternative
EXTERNAL_PORT=8080

# Database Configuration (usually no changes needed for Docker)
POSTGRES_DB=taskmanager
POSTGRES_USER=taskuser
POSTGRES_PASSWORD=taskpass123

# Node Environment
NODE_ENV=production
