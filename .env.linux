# Linux Deployment Environment Configuration
# Copy this file to .env in the project root and set your server's IP address

# Host IP Configuration - CHANGE THIS TO YOUR SERVER'S IP ADDRESS
# Examples:
# HOST_IP=192.168.1.100
# HOST_IP=10.0.0.50
# HOST_IP=172.16.0.10
HOST_IP=localhost

# Alternatively, you can set these individually if needed:
# REACT_APP_API_URL=http://192.168.1.100:3001
# FRONTEND_URL=http://192.168.1.100:3000

# Database Configuration (usually no changes needed for Docker)
POSTGRES_DB=taskmanager
POSTGRES_USER=taskuser
POSTGRES_PASSWORD=taskpass123

# Node Environment
NODE_ENV=production